/* 
// Function to handle deleting a student record by index
const deleteStudent = (index) => {
  const cpyArr = [...student];
  console.log(cpyArr);
  cpyArr.slice(index, 1);
  // setStudent(cpyArr)
  console.log(cpyArr);
};
 */